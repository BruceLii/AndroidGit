<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.stru">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.android.stru.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.android.stru.service.IMService"
            android:enabled="true"
            android:exported="false" />

        <activity android:name="com.android.stru.animations.AnimationActivity" />
        <activity
            android:name="com.android.stru.animations.AnimListActivity"
            android:label="@string/title_activity_anim_list" />
        <activity android:name="com.android.stru.animations.TargetAnimationActivity"></activity>
    </application>

</manifest>